module.exports = function (grunt) {

    this.initConfig({
        mkdir: {
            all: {
                options: {
                    create: ['tmp', 'test/very/deep/folder']
                },
            },
        },
    })

    this.loadNpmTasks('grunt-mkdir');

    return this.registerTask('jaffa', ['mkdir']);
}
